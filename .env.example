# Day 59: copy this file to ".env" (already gitignored) and set a real
# value locally. Never commit the real password — this is exactly the
# "secret goes through an environment variable, never a checked-in file"
# lesson from Day 57, applied for real this time because a Linux SQL Server
# container only supports SQL Authentication (no Windows Authentication).
SA_PASSWORD=Change_Me_Please1
